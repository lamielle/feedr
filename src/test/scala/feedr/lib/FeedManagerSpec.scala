package feedr.lib

import org.scalatest.FlatSpec
import org.scalatest.matchers.ShouldMatchers

class FeedManagerSpec extends FlatSpec with ShouldMatchers {
/*
  "shortestUniqueString" should "handle both empty inputs" in {
    FeedsManager.shortestUniqueString("", Set()) should equal (None)
  }

  "shortestUniqueString" should "handle an empty input string" in {
    FeedsManager.shortestUniqueString("", Set("a")) should equal (None)
    FeedsManager.shortestUniqueString("", Set("a", "b")) should equal (None)
  }

  "shortestUniqueString" should "handle an empty input set" in {
    FeedsManager.shortestUniqueString("a", Set()) should equal (Some("a"))
    FeedsManager.shortestUniqueString("abcd", Set()) should equal (Some("a"))
  }

  "shortestUniqueString" should "work properly" in {
    FeedsManager.shortestUniqueString("a", Set("a")) should equal (None)
    FeedsManager.shortestUniqueString("ab", Set("a", "b", "c")) should equal (Some("ab"))
    FeedsManager.shortestUniqueString("a", Set("c", "b", "c")) should equal (Some("a"))
    FeedsManager.shortestUniqueString("abcdef", Set("d", "b", "c")) should equal (Some("a"))
    FeedsManager.shortestUniqueString("abcdef", Set("abcd", "basdf", "csdf")) should equal (Some("abcde"))
    FeedsManager.shortestUniqueString("abc", Set("abc", "bd", "c")) should equal (None)
    //FeedsManager$.shortestUniqueString("7f2a4745-5d02-4ec3-98cc-c8b964c97c3f", Set("e", "34", "4", "9", "4f", "d6", "2", "7")) should equal (Some("7f"))
    //FeedsManager$.shortestUniqueString("7f2a4745-5d02-4ec3-98cc-c8b964c97c3f", Set("e", "34", "4", "9", "4f", "d6", "c1", "6", "1", "39", "b", "0", "2", "0a", "c", "30", "7", "3", "d")) should equal (Some("7f"))
  }
*/
}
